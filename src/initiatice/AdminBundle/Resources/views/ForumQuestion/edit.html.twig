{% extends "initiaticeAdminBundle::layout.html.twig" %}
{% block initiaticeAdmin_body %}
    <h3>Editer la question</h3>
    {{ form_start(form) }}

    <div class="row">
        <div class="input-field col s10">{{ form_row(form.title) }}</div>
        <div class="input-field col s2">{{ form_row(form.userId) }}</div>
    </div>
    <div class="input-field">{{ form_row(form.content) }}</div>

    {{ form_row(form.save, {'attr': {'class': 'btn waves-effect waves-light'}} ) }}
    {{ form_end(form) }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}